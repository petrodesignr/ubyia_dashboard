<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style/client.css">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />

    <title>Détail Client</title>
</head>

<body>
    <%- include('../header.ejs') %>
        <% if (staff.id_role===1) { %> <!--vérifie le niveau du membre du staff est bien à 1 pour voir le bouton-->
            <div class="buttons-container">
                <label for="delete-confirm" class="deleteOne" style="min-width: 65px;">Supprimer le client</label>
                <input type="checkbox" id="delete-confirm" style="display: none;">
                <% } else { %>
                    <% } %>
 
                        <!-- Pop-up -->
                        <div class="modal">
                            <div class="modal-content">
                                <h2>Êtes-vous sûr de vouloir supprimer ce client ?</h2>
                                <form action="/client/detailClient/<%= clients.company_id %>?_method=DELETE"
                                    method="POST" class="inline-form">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <button class="delete" type="submit">Oui, supprimer</button>
                                </form>
                                <label for="delete-confirm" class="cancel">Annuler</label>
                            </div>
                        </div>
            </div>
            <% if (typeof message !=='undefined' ) { %>
                <div class="alert alert-danger" role="alert">
                    <%= message %>
                </div>
                <% } %>

                    <form id="form" action="/client/detailClient/<%= clients.company_id %>?_method=PUT" method="POST">
                        <input type="hidden" name="_method" value="PUT">
                        <h2 style="text-align: center;">Client #<%= clients.company_id %>
                        </h2>
                        <div class="separation"></div>

                        <div class="form-group">
                            <label for="gender" id="gender">Genre
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a></label>
                            <div class="gender-options">
                                <div class="gender-items">
                                    <label for="male">Homme</label>
                                    <input type="radio" id="male" name="company_gender" value="homme" <%=isEditable ? ''
                                        : 'disabled' %>
                                    <% if (clients.company_gender==='homme' ) { %> checked <% } %> >
                                </div>
                                <div class="gender-items">
                                    <label for="female">Femme</label>
                                    <input type="radio" id="female" name="company_gender" value="femme" <%=isEditable
                                        ? '' : 'disabled' %>
                                    <% if (clients.company_gender==='femme' ) { %> checked <% } %> >
                                </div>

                            </div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="lastname">Nom</label>
                            <div class="align">
                                <input type="text" id="lastname" name="company_lastname"
                                    value="<%= clients.company_lastname %>" <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="firstname">Prénom</label>
                            <div class="align">
                                <input type="text" id="firstname" name="company_firstname"
                                    value="<%= clients.company_firstname %>" <%=isEditable ? '' : 'disabled' %>
                                >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="position">Poste</label>
                            <div class="align">
                                <input type="text" id="position" name="company_position"
                                    value="<%= clients.company_position %>" <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="email">Email</label>
                            <div class="align">
                                <input type="email" id="email" name="company_email" value="<%= clients.company_email %>"
                                    <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="name">RS Social</label>
                            <div class="align">
                                <input type="text" id="name" name="company_name" value="<%= clients.company_name %>"
                                    <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="phone">Numéro de téléphone</label>
                            <div class="align">
                                <input type="tel" id="phone" name="company_phone" value="<%= clients.company_phone %>"
                                    <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="siret">Siret</label>
                            <div class="align">
                                <input type="text" id="siret" name="company_siret" required
                                    value="<%= clients.company_siret %>" <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span>
                                </a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="address">Adresse</label>
                            <div class="align">
                                <input type="text" id="address" name="company_address" required
                                    autocomplete="address-level1" value="<%= clients.company_address %>" <%=isEditable
                                    ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span></a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="postal_code">CP</label>
                            <div class="align">
                                <input type="text" id="postal_code" name="company_postal_code" required
                                    value="<%= clients.company_postal_code %>" <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span></a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>

                        <div class="form-group">
                            <label for="city">Ville</label>
                            <div class="align">
                                <input type="text" id="city" name="company_city" required
                                    value="<%= clients.company_city %>" <%=isEditable ? '' : 'disabled' %> >
                                <a href="/client/detailClient/<%= clients.company_id %>?edit=true" class="edit-link">
                                    <span class="material-symbols-outlined">edit_square</span></a>
                            </div>
                            <div class="separation"></div>
                            <br>
                        </div>
                        <a href="/server/addClient/<%= clients.company_id %>" class="link">Rattacher ce client à
                            un/des serveurs</a>
                        <div class="buttons-content">
                            <button class="update" type="submit">Mettre à jour</button>
                        </div>
                    </form>

</body>

</html>